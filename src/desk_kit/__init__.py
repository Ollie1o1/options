"""Desk kit: the shared render layer for every HTML report the desk emits.

One theme, one shell, one chart library, one hub — so the tearsheet, the
research desk and the morning briefing read as one product instead of three
that merely rhyme. Everything here is a pure function over plain data; all
I/O stays in each report's __main__ and in hub.write_index.
"""
